# Название Compose-проекта
COMPOSE_PROJECT_NAME=sql-lab

# Закреплённые версии СУБД и версия UI
MYSQL_VERSION=9.7.1
POSTGRES_VERSION=18.4
CLICKHOUSE_VERSION=26.3.17.56
ADMINER_VERSION=5.4.2

# Публикация портов только на loopback по умолчанию.
# При необходимости можно явно указать другой адрес, например адрес VPN-интерфейса.
BIND_ADDRESS=127.0.0.1

# Порты хоста
MYSQL_PORT=3306
POSTGRES_PORT=5432
CLICKHOUSE_HTTP_PORT=8123
ADMINER_PORT=8081

# Данные, конфигурация, обязательный init и опциональные samples
MYSQL_DATA_DIR=./data/mysql
POSTGRES_DATA_DIR=./data/postgres
CLICKHOUSE_DATA_DIR=./data/clickhouse
MYSQL_CONF_FILE=./conf/mysql/my.cnf
MYSQL_INITDB_DIR=./initdb/mysql
POSTGRES_INITDB_DIR=./initdb/postgres
CLICKHOUSE_INITDB_DIR=./initdb/clickhouse
MYSQL_SAMPLES_DIR=./samples/mysql
POSTGRES_SAMPLES_DIR=./samples/postgres

# Обязательные учебные базы
MYSQL_DATABASE=demo
POSTGRES_DATABASE=demo
CLICKHOUSE_DATABASE=demo

# Локальные административные credentials.
# Не используйте эти значения в публичном или production-окружении.
MYSQL_ROOT_PASSWORD=root
POSTGRES_SUPERUSER=postgres
POSTGRES_SUPERUSER_PASSWORD=postgres
CLICKHOUSE_ADMIN_USER=clickhouse_admin
CLICKHOUSE_ADMIN_PASSWORD=clickhouse_admin

# Общие локальные credentials учебного пользователя трёх СУБД.
DB_USER=student
DB_PASSWORD=student

# Внешняя сеть при необходимости.
# EXTERNAL_NETWORK=external
